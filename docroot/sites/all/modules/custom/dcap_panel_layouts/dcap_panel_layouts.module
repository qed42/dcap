<?php

/**
 * @file
 * Module file for the layouts defining.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function dcap_panel_layouts_ctools_plugin_directory($owner, $plugin_type) {
  if ($owner == 'panels' && $plugin_type == 'layouts') {
    return "plugins/{$plugin_type}";
  }
}
